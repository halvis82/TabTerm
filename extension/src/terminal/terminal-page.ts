/**
 * Terminal page: one data connection, multiplexing every pane in this tab.
 *
 * Attaches lazily on first visibility, because Chrome restores every tab at once at startup.
 * See docs/04-session-lifecycle.md.
 */
export {};
